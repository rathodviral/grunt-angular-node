(function () {
    'use strict';
    angular.module('app.contact').controller('contactController', contactController);

    contactController.$inject = ['$timeout'];

    function contactController($timeout) {
        /*jshint validthis: true */
        var vm = this;

    }
})();
